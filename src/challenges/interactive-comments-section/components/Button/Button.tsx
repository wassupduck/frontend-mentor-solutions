import styles from "./Button.module.css";

import UnstyledButton from "../../../../components/UnstyledButton";
import styled from "../../../../styled";

const Button = styled(UnstyledButton, styles.button);

export default Button;
